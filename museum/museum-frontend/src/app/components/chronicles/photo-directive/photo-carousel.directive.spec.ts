import { PhotoCarouselDirective } from './photo-carousel.directive';

describe('PhotoCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new PhotoCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
